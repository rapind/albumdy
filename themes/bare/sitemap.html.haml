%h1
  Sitemap
%ul
  %li
    %h2= link_to 'Home', root_path, :title => 'Home'
    %h3 Albums
    %ul
      - for album in @albums
        %li= link_to h(album.title), album_path(album), :title => h(album.title)