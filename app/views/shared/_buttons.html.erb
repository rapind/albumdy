<script type="text/javascript">
$(function() {
  // remove thickbox
	$('p a#cancel').click(function() {
	  
	  // TODO - detect if we're in a thickbox
	  // only call tb_remove(); if we're in a thickbox
	  return tb_remove();
	});
});
</script>
<p class="buttons">
  <button type="submit" class="button positive">
    <%= image_tag 'icons/key.png' %> <%= action%>
  </button>
  <a class="button negative" href="<%= collection_url %>" id="cancel">
    <%= image_tag 'icons/cross.png' %> Cancel
  </a>
</p>