<% form_for :user, :url => {:action => "reset_password"} do |f| %>
  <p>
    <%= f.password_field :password, :class => 'text' %> &nbsp; <%= f.label(:password) %>
	</p>
	<p>
    <%= f.password_field :password_confirmation, :class => 'text' %> &nbsp; <%= f.label(:confirm_password) %>
  </p>
  <p class="buttons">
    <button type="submit" class="button positive">
      <%= image_tag 'icons/key.png', :alt => '' %> &nbsp;Reset Password &raquo;
    </button>
  </p>
<% end %>